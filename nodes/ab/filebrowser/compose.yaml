services:
  filebrowser:
    image: filebrowser/filebrowser:latest
    container_name: filebrowser
    networks:
      - network-layer
    restart: always
    labels:
      - traefik.enable=true
      - traefik.http.routers.filebrowser.rule=Host(`files.ab18.in`)
      - traefik.http.routers.filebrowser.middlewares=auth
      - homepage.group=Local Utilities
      - homepage.name=File Browser
      - homepage.icon=https://cdn.jsdelivr.net/gh/homarr-labs/dashboard-icons/png/filebrowser-quantum.png
      - homepage.href=https://files.ab18.in
      - homepage.description=File Manager
    env_file: /home/dietpi/localstack/secrets/ab-filebrowser.env
    volumes:
      # This exposes your localstack directory to the app
      - /home/dietpi/localstack/data/filebrowser_data/srv:/srv
      - /home/dietpi/localstack:/srv/localstack
      - /home/dietpi/music:/srv/music
      # Data persistence for FileBrowser settings/users
      - /home/dietpi/localstack/data/filebrowser_data:/database
      - ./config:/config

networks:
  network-layer:
    name: tailscale_bridge
    external: true
